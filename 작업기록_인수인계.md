# momuri 작업기록 · 인수인계

> 이 파일은 다음에 이어서 작업하려고 남기는 상태 기록이에요.
> 다음 세션에서 Claude에게 이 파일을 보여주면 바로 이어서 진행할 수 있어요.
> (마지막 업데이트: 2026-07-07 — **하루 코스(대중교통 포함) 완성** 반영)
>
> **⚠️ 이 저장소만 있으면 어느 컴퓨터서든 이어서 가능** — 전부 GitHub(uurimii16/momuri)에 있고 Netlify 자동배포. 이 맥북 없어도 됨. `git clone` 후 작업.
> **ODsay 키(대중교통 재생성용)는 공개 저장소라 커밋 안 함** → 별도 보관 필요(발급처 lab.odsay.com에서 재확인 가능). 없어도 웹은 이미 대중교통 데이터가 구워져 있어 정상 작동.

## 0. 한 줄 요약
2026 관광데이터 활용 공모전 **②-2 웹·앱 구현 부문 / 지정과제 4번** 출품작 "**momuri(머무리)**".
쇠퇴 소도시 **관광 활성화**를 메인으로, 숨은 매력지(쇠퇴×문화유산) 매칭 → 동선 보조 → 공사 TourAPI 콘텐츠 → 체류·재방문(관계인구).

## 1. 위치 / 저장소 / 배포
- **깃허브**: https://github.com/uurimii16/momuri (public)
- **🌐 배포 링크**: **https://momuri.netlify.app/** (공개, 누구나 접속)
- **로컬 폴더**: `D:\코딩작업\tourgo-local-relation`
- 시작점: 포스터 프로젝트 `tourgo-tourism-decline`를 복사해 시작(분석 엔진 재활용).
- **GitHub CLI(gh) 2.96.0 설치 완료** (`C:\Program Files\GitHub CLI\gh.exe`). 새 터미널에서 `gh` 바로 사용 가능.

## 2. 필수 규정 (반드시 지킬 것)
- **한국관광공사 OpenAPI 필수** — 미충족 시 심사 제외.
- **TourAPI 4.0 KorService2** 사용(키 발급·작동 확인됨).
- **API 키는 커밋 금지** → 환경변수 `TOURAPI_KEY`로 관리. (data.go.kr 개인키도 placeholder로 제거됨)
- 접수 마감: **2026-07-21 16:00**(신청+기획서). 개발 ~9월, 기능심사 10월.

## 3. 현재 상태 (2026-07-06)
### 완성된 것
- **분석 파이프라인** 완성: 쇠퇴지수 → 대도시 필터 → 숨은매력지 후보 → TourAPI 콘텐츠 → choropleth(Jenks).
- **3페이지 웹사이트** (`map/`):
  - `momuri.html` — 숨은매력지 12개 지도 데모(메인)
  - `decline-map.html` — 시군구 쇠퇴지수 단계구분도(Jenks 자연분류, 실제 지수 범위 범례, 하단 설명)
    - **[2026-07-06 개선]** ①클릭 패널에 **방문·매력·연결 3축 분해** + "주된 쇠퇴 요인" 표시(지수 차별점을 지도가 증명) ②GeoJSON **9.2MB→0.67MB**(shapely 단순화+캐시 정상화) ③hover 하이라이트·패널 닫기·빈곳 클릭 닫힘 ④**쇠퇴 상위 Top15 랭킹**(클릭 시 flyTo). 3축값은 방법론 예시(진천 매력+4.70 등)와 일치 검증됨.
  - `course.html` — **[2026-07-07 신규] 하루 코스 타임라인**: 명소·맛집을 하루 리듬으로 묶은 동선. 번호 스톱(시간·머무름·사진) + 지도 실도로 경로선 + **자동차/대중교통 토글**. 대중교통은 버스번호·환승·구간분(ODsay), 경로 없는 구간은 "🚗 자동차 권장". 데이터 `data/courses.json`(12코스). 초록 핀 팝업/내비/랜딩카드에서 진입.
  - `methodology.html` — 지표 방법론 설명서
  - 밝은 테마 + 머무리 로고(인라인 SVG) + 한국관광공사 TourAPI 출처 표시.
- **루트 랜딩** `index.html`(3페이지 진입) + 페이지 간 내비게이션.
- **✅ Netlify 배포 완료** — GitHub 연동, `netlify.toml`(publish=루트). `git push` 하면 **자동 재배포**. 지도(Routo)도 공개에서 정상 표시 확인.
- **README.md 웹앱 기준으로 재정리** + `requirements.txt`(재현용) 추가. 다른 컴퓨터에서 클론만 하면 웹 즉시 작동.

### 실행 / 배포
```bash
# 로컬 미리보기
cd map && python -m http.server 8901   # http://localhost:8901/momuri.html (포스터는 8899)

# 배포 갱신 (Netlify 자동)
git push        # → 1분 뒤 https://momuri.netlify.app/ 자동 반영
```

## 4. 저장소 구조
```
├─ index.html             배포용 루트 랜딩(3페이지 진입)
├─ netlify.toml           Netlify 배포 설정(publish=루트)
├─ README.md              웹앱 셋업·재현 가이드(정본)
├─ requirements.txt       분석 재현용 파이썬 패키지
├─ 작업기록_인수인계.md      ← 이 파일
├─ docs/
│  ├─ 웹앱_기획_과제4.md     핵심 기획서 (개념·3층 구조·전략)
│  ├─ 보고서_골격.md / 보고서_초안.md / 인용자료_정리.md   (포스터 자산)
├─ figures/               분석 그림 4종
├─ map/                   웹사이트 (momuri/course/decline-map/methodology.html + data/ + 구 app.js·style.css)
│                         data/: momuri.json · courses.json · decline_choropleth.geojson ...
└─ analysis/              분석 코드 + 산출물 (build_*.py[+build_courses], pull_*.py, tourapi_fetch.py)
```
> 제외(.gitignore): `.sav .xlsx .pdf .zip`, `map/data/sigungu.geojson`(18MB 경계, 재다운로드 가능). 웹 작동엔 불필요(계산된 산출물은 커밋됨).

## 5. 데이터 파이프라인 (재현 순서)
```bash
pip install -r requirements.txt   # pandas numpy pyreadstat networkx
# 환경변수: TOURAPI_KEY(필수), DATAGO_KEY(방문자수)
python analysis/pull_visits.py        # ① 방문 → visits.json
python analysis/build_index.py        # ②매력·③연결 (knts2025.sav 필요) → index_axes.json
python analysis/combine_index.py      # 복합쇠퇴지수 → index_full.json
python analysis/build_candidates.py   # 숨은매력지 후보(대도시 필터) → candidates.json
python analysis/build_choropleth.py   # 단계구분도(Jenks) (sigungu.geojson 필요) — 3축 출력+shapely 단순화 반영
python analysis/build_service_data.py 12   # TourAPI 콘텐츠 → map/data/momuri.json
```
※ 산출물이 이미 커밋돼 있어 웹만 볼 거면 생략 가능.
※ `sigungu.geojson`(18MB) 재다운로드 없이 기존 geojson만 후처리(3축 주입·단순화)하려면 → `python analysis/patch_choropleth.py` (requirements: shapely).

## 6. 다음 할 일 (TODO)
1. **테마 우선순위 / 미식 지수** — 문화유산+미식 추천. 미식형 소도시 랭킹 지수 제작 여부 결정.
2. **머무리 로고 다듬기** — 현재 임시 SVG(핀+집).
3. **축제 데이터 이슈** — TourAPI `searchFestival2` 0건 반환. 파라미터·기간·지역코드 점검("지금 갈 이유" 채우기).
4. ~~동선 연계 코스 + 대중교통~~ **[2026-07-07 완성]** `course.html` + `analysis/build_courses.py` → `courses.json`(12코스, 자동차 OSRM + **대중교통 ODsay 채움 완료**: 8곳 전구간·4곳 부분, 나머지 자동차권장).
   - **코스 데이터 재생성 방법**(원자료 바뀔 때만): `ODSAY_KEY='발급키' ODSAY_REFERER='https://momuri.netlify.app' python3 analysis/build_courses.py && git push`. 키 없으면 자동차만. 키는 ODsay 앱 등록 도메인을 Referer로 붙여 통과(서버측 호출).
   - 키 발급처: **lab.odsay.com** → 앱 등록(서비스 URL=배포도메인) → 웹서비스 키. 무료 일 1000건. 오퍼레이션 `searchPubTransPathT`.
   - 코스 골격: `PLAN`(명소80분→점심맛집60→명소80→명소60→저녁맛집70). 출발 09:30, 시각은 프론트에서 모드별 계산.
   - 남은 개선 여지: 스톱 수/시간대 튜닝, 카페 카테고리 분리(현재 맛집만), 전국 확장, 사용자가 직접 담기.
5. **일자리·체류 매칭** — 관계인구→생활인구 심화(워크넷 등, Phase 3).
6. **지도 좌표 전체 확장** — 숨은매력지 12곳 → 전국.

## 7. 사용자 메모
- 코딩 초보 → 단계별 친절한 설명 필요.
- 원본 보존 + 복사분리 방식 선호(포스터 원본은 `tourgo-tourism-decline`에 그대로 둠).
- 배포는 정적 사이트(Streamlit 아님). 수정 = 로컬 고치고 `git push` = 자동 갱신.
