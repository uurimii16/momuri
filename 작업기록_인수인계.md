# momuri 작업기록 · 인수인계

> 이 파일은 다음에 이어서 작업하려고 남기는 상태 기록이에요.
> 다음 세션에서 Claude에게 이 파일을 보여주면 바로 이어서 진행할 수 있어요.
> (마지막 업데이트: 2026-07-06)

## 0. 한 줄 요약
2026 관광데이터 활용 공모전 **②-2 웹·앱 구현 부문 / 지정과제 4번** 출품작 "**momuri(머무리)**".
쇠퇴 소도시 **관광 활성화**를 메인으로, 숨은 매력지(쇠퇴×문화유산) 매칭 → 동선 보조 → 공사 TourAPI 콘텐츠 → 체류·재방문(관계인구).

## 1. 위치 / 저장소
- **깃허브**: https://github.com/uurimii16/momuri (public)
- **로컬 폴더**: `D:\코딩작업\tourgo-local-relation`
- 시작점: 포스터 프로젝트 `tourgo-tourism-decline`를 복사해 시작(분석 엔진 재활용).
- **GitHub CLI(gh) 2.96.0 설치 완료** (`C:\Program Files\GitHub CLI\gh.exe`). 새 터미널에서 `gh` 바로 사용 가능.

## 2. 필수 규정 (반드시 지킬 것)
- **한국관광공사 OpenAPI 필수** — 미충족 시 심사 제외.
- **TourAPI 4.0 KorService2** 사용(키 발급·작동 확인됨).
- **API 키는 커밋 금지** → 환경변수 `TOURAPI_KEY`로 관리. (data.go.kr 개인키도 placeholder로 제거됨)
- 접수 마감: **2026-07-21 16:00**(신청+기획서). 개발 ~9월, 기능심사 10월.

## 3. 현재 상태 (2026-07-06)
### 완성된 것
- **분석 파이프라인** 완성: 쇠퇴지수 → 대도시 필터 → 숨은매력지 후보 → TourAPI 콘텐츠 → choropleth.
- **3페이지 웹사이트** (`map/`):
  - `momuri.html` — 숨은매력지 12개 지도 데모(메인)
  - `decline-map.html` — 시군구 쇠퇴지수 단계구분도(Jenks 범위 범례)
  - `methodology.html` — 지표 방법론 설명서
  - 밝은 테마 리디자인 + 머무리 로고(SVG) + 관광공사 출처 표시 완료.
- 로컬 서버 포트: **8901** (예전 tourgo 포스터는 8899).

### 실행 방법
```bash
# 웹사이트 미리보기 (map 폴더 안에서)
cd map
python -m http.server 8901
# 브라우저에서 http://localhost:8901/momuri.html
```

## 4. 저장소 구조
```
├─ README.md              (포스터 프로젝트 서사 — 웹앱용으로 추후 정리 필요)
├─ 작업기록_인수인계.md      ← 이 파일
├─ docs/
│  ├─ 웹앱_기획_과제4.md     핵심 기획서 (개념·3층 구조)
│  ├─ 보고서_골격.md / 보고서_초안.md
│  └─ 인용자료_정리.md
├─ figures/               분석 그림 4종
├─ map/                   웹사이트 (momuri/decline-map/methodology.html + app.js + style.css)
└─ analysis/              분석 코드 + 산출물 (build_*.py, pull_*.py, tourapi_fetch.py)
```
> 참고: `.sav .xlsx .pdf .zip`, `map/data/sigungu.geojson`(18MB)는 `.gitignore`로 제외됨(깃허브에 안 올라감).

## 5. 다음 할 일 (TODO)
1. **테마 우선순위 확정** — 문화유산+미식 추천 방향. (미식 지수를 별도로 만들지 여부 결정)
2. **서비스명 확정** — 현재 "momuri(머무리)".
3. **축제 데이터 이슈** — TourAPI `searchFestival2`가 0건 반환. 원인 파악 필요(파라미터·기간·지역코드).
4. **지도 좌표 전체 확장** — 현재 숨은매력지 12곳 → 전국으로 확장.
5. README를 포스터 서사에서 **웹앱(과제4) 기준**으로 다시 정리.

## 6. 사용자 메모
- 코딩 초보 → 단계별 친절한 설명 필요.
- 원본 보존 + 복사분리 방식 선호(포스터 원본은 `tourgo-tourism-decline`에 그대로 둠).
